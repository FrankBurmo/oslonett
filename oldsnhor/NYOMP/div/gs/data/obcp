#!/local/bin/perl5 -w

$table = shift || die "Usage: $0 <table>\n";
$table =~ s/\.\w+$//;  # remove suffix

$file = "$table.bcp";
$err  = "$table.err";

$cmd = "bcp $table in $file -e $err -c -Uoslonett -Poslonett";

print "$cmd\n";
system $cmd;
