#!/bin/sh

# 

PATH=/usr/bin:/bin:/local/bin:/usr/ucb
TMP=$$.tmp

echo "Kopierer tilbake fra .bak filer slik at doit kan kj|res p} nytt" 
for x in *.html.bak
do
  cp $x `basename $x .bak`
  echo $x kopiert tilbake
done
echo "[ferdig]"


